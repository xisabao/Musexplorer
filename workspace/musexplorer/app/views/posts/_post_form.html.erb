
<div class="page-container">
<%= form_for([@category, @post], html: { class: "post-form" }) do |f| %>
	<%= f.label :title %>
	<%= f.text_field :title %>
	
	<%= f.label :body %>
	<%= f.text_area :body, rows: 8 %>

	<%= f.submit "Post" %>
<% end %>
</div>